In order to run tests:
1) pip install lxml
2) python test.py

- XML files to be tested should be in xml_dir
- after running test.py test_result contains .txt file with errors in xml with analogical name
- if test_result is empty then all tests work
